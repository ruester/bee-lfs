#!/bin/env beesh
require_version 1.0_rc17

CPUS=$(grep -c processor /proc/cpuinfo)
SRCURL[0]="/usr/src/automake-1.11.1.tar.bz2"

build_in_sourcedir

mee_configure() {
    ${S}/configure --prefix=/usr --docdir=/usr/share/doc/automake-1.11.1
}

mee_build() {
    start_cmd make -j $CPUS
}

mee_install() {
    start_cmd make DESTDIR=${D} install
}
