#!/bin/env beesh
require_version 1.0_rc17

CPUS=$(cat /proc/cpuinfo | grep processor | wc -l)
SRCURL[0]="/usr/src/man-pages-3.35.tar.gz"

build_in_sourcedir

mee_configure() {
    true
}

mee_build() {
    true
}

mee_install() {
    make DESTDIR=${D} install
}
