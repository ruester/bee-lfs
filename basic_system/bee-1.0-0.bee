#!/bin/env beesh

require_version 1.0_rc17

SRCURL[0]="/usr/src/bee.tar"
PATCHURL[0]=""

build_in_sourcedir

mee_build() {
    true
}

mee_install() {
    start_cmd make DESTDIR=${D} PREFIX=/usr install
}
