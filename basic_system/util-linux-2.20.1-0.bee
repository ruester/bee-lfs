#!/bin/env beesh

require_version 1.0_rc23

SRCDIR=/sources
SRCURL[0]="${SRCDIR}/${PKGFULLNAME}-${PKGVERSION}.tar.bz2"

build_in_sourcedir

mee_patch_pre() {
    sed -e 's@etc/adjtime@var/lib/hwclock/adjtime@g' \
        -i $(grep -rl '/etc/adjtime' .)
}

mee_configure() {
    start_cmd ${S}/configure --enable-arch \
        --enable-partx \
        --enable-write
}

mee_install_post() {
    mkdir -pv ${D}/var/lib/hwclock
}
