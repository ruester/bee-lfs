#!/bin/env beesh

require_version 1.0_rc23

BEE_MAKEFLAGS="-j $(grep -c processor /proc/cpuinfo)"
SRCDIR=/sources
SRCURL[0]="${SRCDIR}/${PKGFULLNAME}-${PKGVERSION}.tar.bz2"

LIBEXECDIR=/usr/sbin
BINDIR=/bin

build_in_sourcedir

mee_configure() {
    FORCE_UNSAFE_CONFIGURE=1 bee_configure
}
