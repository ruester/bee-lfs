#!/usr/bin/env beesh

require_version 1.0_rc23

SRCDIR=/mnt/lfs/sources
SRCURL[0]="${SRCDIR}/tar-${PKGVERSION}.tar.bz2"
PREFIX=${LFS_PREFIX=/tools}
export PATH=${PREFIX}/bin:/bin:/usr/bin

build_in_sourcedir

mee_configure() {
    bee_configure FORCE_UNSAFE_CONFIGURE=1
}
