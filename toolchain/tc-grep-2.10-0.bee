#!/usr/bin/env beesh

require_version 1.0_rc23

SRCDIR=/mnt/lfs/sources
SRCURL[0]="${SRCDIR}/grep-${PKGVERSION}.tar.xz"
PREFIX=${LFS_PREFIX=/tools}
export PATH=${PREFIX}/bin:/bin:/usr/bin

build_in_sourcedir

mee_configure() {
    bee_configure --disable-perl-regexp
}
